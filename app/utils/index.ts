// Dùng cho việc hiển thị badge trạng thái
export const statusColors: Record<string, any> = { published: 'green', 'on-hold': 'gray' }
export const statusLabels: Record<string, string> = { published: 'Đã xuất bản', 'on-hold': 'Tạm ngưng' }
